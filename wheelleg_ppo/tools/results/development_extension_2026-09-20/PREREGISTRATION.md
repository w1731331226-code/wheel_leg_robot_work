# 开发扩展评估预注册（2026-09-20）

目的：在训练前判断原12例以外、既定同分布内B0是否仍饱和。此处是开发诊断，不是新的正式困难验证集或最终测试，不证明学习方法优势。

运行前固定：现有 sample_scenario('validation', seed, stage=3)，连续种子1000～1023，共24例；不按结果筛选、补抽或增加难度。沿用 preppo-v2-ramp、全部成功阈值、名义控制器和既有范围；组合保留区域继续排除，所有 test_* 划分不调用。Actor延迟对零残差B0没有影响，不能据此声称延迟鲁棒性。

完整24例进入分母。先保存全部场景与历史基线对应源码哈希，再运行；结束核对哈希未变。主报告成功数、失败原因及逐例既有指标，失败连续指标仅用于诊断。

预定解释：若成功≥22/24，哪怕候选方法全成功，相对B0的提升也不足10个百分点，仍不允许启动长训练；若成功≤21/24，仅表明样本上有算术空间，须先诊断失败和确认任务可行性，再独立冻结正式验证协议及B1比较，不能自动放行训练或选取失败子集。样本小，任何结果都不证明总体饱和或PPO可改善。

复现：在项目根目录执行 `OPENBLAS_NUM_THREADS=1 OMP_NUM_THREADS=1 .venv/bin/python wheelleg_ppo/tools/results/development_extension_2026-09-20/evaluate_extension.py`。脚本拒绝覆盖已完成结果；复验应将脚本复制到同层新结果目录。

## 执行结果（预注册内容固定后追加）

完整24例成功23例（95.83%），失败1例。development_1023完成行驶和停车，但yaw峰值5.186011°超过5°，因此success=false；日志reason=completed表示运行流程结束，并不表示通过全部成功判据。该例右侧凸台19.888 mm、速度−0.607576 m/s，实际右凸台接触已记录。失败时残差为零；现有饱和统计不能直接解释成学习残差生效或失败因果。

全部24例的最大相对速度RMSE为13.3021%（门槛20%），最大停车距离0.261134 m（门槛0.60 m），最大末段速度0.004792 m/s（门槛0.03 m/s）。全部逐例指标，包括失败轨迹和接触摩擦，见baseline.json；未只汇总成功轨迹。

脚本退出码0，运行前后历史基线清单的18个文件哈希一致；场景清单先于仿真写入，24例全部进入分母。控制器、环境、协议阈值未修改，未调用最终测试划分或启动训练。

依预定解释关闭本轮开发诊断：相对B0最大可能提升仅4.17个百分点，原10个百分点继续门仍不可用。该结论仅限固定样本，不证明总体无改进空间。后续先决定研究问题及独立验证协议，不能据本次结果挑失败集、调参后回报同组为独立证据，或自动把yaw RMS升为主指标。
