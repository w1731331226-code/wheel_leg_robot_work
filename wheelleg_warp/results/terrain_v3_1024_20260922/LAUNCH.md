# terrain-v3 正式训练启动记录

正式训练从terrain-v2第2轮末点检查点恢复，不使用准入探针权重。配置为1024环境、n_steps=50、每轮1,024,000策略步、最多10轮、连续3轮无改善停止；每轮中点与末点评估，排序依次为总失败、原回归失败、混合地形失败和Jψ。

训练服务 `wheelleg-terrain-v3-formal.service` 使用24GiB内存上限并禁用换页；独立仪表盘和渲染服务为 `wheelleg-terrain-v3-dashboard.service`、`wheelleg-terrain-v3-render.service`。页面为 `http://127.0.0.1:8768/`，数据根为 `dashboard/local_data/terrain_v3/`，不覆盖原基线或v1/v2动画。

启动后已看到1024总览、环境111混合地形真实3D详情、约47.7 FPS显示/400 Hz采集和第1轮512,000步中点评估。最终64例留出只在停止后打开。

第1轮已完整结束并进入第2轮：选定检查点为开发47/48、原回归16/16，较起点46/48和15/16把总失败从3降到1，停滞计数为0。训练继续遵守冻结停止规则。

最终训练共6轮，第3轮中点成为最佳；第4～6轮连续3轮未改善后停止。停止后首次留出验收为47/64、61/64完整且有通过证据，原回归16/16。渲染服务已停止释放资源，8768仪表盘保留最终快照；完整结论见`REPORT.md`。
