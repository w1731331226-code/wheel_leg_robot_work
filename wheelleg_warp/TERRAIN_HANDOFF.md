# 非结构化地形任务交接（2026-09-23）

## 当前结论

项目处于**任务契约 v2 下的策略能力诊断阶段**，不是正式训练阶段。已修复逐世界静态地形、通过证据和成功/奖励/停车不一致等确定性问题；现有 terrain-v3 M3 检查点没有相对原 GPU M3 检查点取得稳定、大幅的困难台阶收益。本轮没有晋升权重、改变默认控制或打开 720000～723999 全新留出；不要把“再训练一轮”当作下一步。

证据以 [项目记忆](../PROJECT_MEMORY.md) 顶部当前状态、[地形计划](TERRAIN_PLAN.md) 的“接触前动作时序与干预已完成”段、[任务契约 v2 基线](results/contract_v2_baseline_checked_20260923/REPORT.md) 和 [接触前动作试验](results/hard_prefix_probe_v2_20260923/REPORT.md) 为准。旧计划按时间累积，早期“下一步”不覆盖这些最新结论。检查点路径、场景面板、源码与归一化哈希见各结果目录的 `protocol.json`，不要靠目录日期推断协议相同。

## 已验证事实与证据边界

| 对照 | 48 例困难台阶成功 | 完成 | 解释 |
|---|---:|---:|---|
| 任务契约 v2：terrain-v3，三次 | 32、32、33 | 43、41、45 | 160 个公开开发世界，同 GPU 后端与任务契约 |
| 任务契约 v2：原 GPU，三次 | 35、35、35 | 43、44、42 | 同批配对；两个策略的 mixed 每次均 12/12 |
| 全 48 例前缀试验 A：terrain-v3 全程，两次 | 34、34 | 44、43 | 其余 112 世界始终 terrain-v3 |
| B：首次轮地形接触前用原 GPU 动作，随后 terrain-v3 | 34、35 | 41、44 | 用模拟器特权接触位切换，非可部署的 32 维观测策略 |
| C：48 例困难世界全程原 GPU 动作 | 35、35 | 43、42 | 不能由少数成功场景替代完整能力判断 |

两例定向试验中，330083 的 A 为 0/2、B 为 2/2；21 mm、0.5 m/s 的 700102 为 1/2、2/2。但全困难组中，21 mm 低速累计从 5/8 到 8/8 的同时，24 mm 低速从 2/8 降到 1/8，27 mm、0.7 m/s 从 4/8 降到 2/8。每格是 **4 个场景×2 次相关回放**，不能当作 8 个独立样本。统一借用原 GPU 前缀已被完整组否定，不应晋升或包装成策略优化。

[2 kHz 遥测与图](results/policy_trace_pair_v2_20260923/seed_700102_contact_timeline.png)显示：一次带仪器的 700102 回放中，terrain-v3 双轮初次触阶相隔 71.0 ms，原 GPU 为 21.5 ms；轮纵向位置差约 3.8/1.2 cm。terrain-v3 的偏航先升至 2°，随后出现明显轮速包络限幅，最终越过 5°。这支持“接触前动作—双轮错相—偏航放大”的**线索**，没有证明某个动作通道或轮限幅是唯一根因。该仪器回放中原 GPU 的 330083 也失败，不能把它与普通基线中三次成功拼作同一次因果对照。原始 NPZ、50 Hz 动作/观测、归一化逐值核验和 160 世界终态均在 [遥测目录](results/policy_trace_pair_v2_20260923/)；ABCCBA 原始结果分别在 [两例目录](results/prefix_action_probe_v2_20260923/) 和 [全困难组目录](results/hard_prefix_probe_v2_20260923/)。

同输入零残差的两个 160 世界 GPU 环境在首个 20 ms 已有接触记录顺序和极小状态差异，73/160 世界不逐位相同；闭环部分场景会翻转结局。目前只定位到**最早可见差异**，未证明原子求解顺序是唯一原因。单次或小幅成功率变化、跨不同测试集的分数，以及带仪器/不带仪器的轨迹拼接，都不足以证明能力进步。早期共享静态地形缺陷影响的 55/64、60/64 不能作泛化证据；旧 terrain-v3 最终 64 已公开，也不能再当新留出选模。

## 接手后按此顺序推进

1. **先证明 24～27 mm 的受约束可达性。** 保持现有速度命令、M3/电机执行包络、世界姿态门和任务契约 v2；从已公开困难开发场景分层选 24/27 mm、不同速度及其原场景对照。记录接触前轮位置/速度、左右首次接触、请求与实际残差、共同缩放 λ、力矩/轮速限幅、首次姿态越界和终止原因。只做短时、可回放的动作探针；找到可重复成功的合法动作，再讨论如何学习它。若合法动作本身不可达，应报告约束瓶颈，而不是先加 PPO 步数或暗改阈值。
2. **把数值波动与行为效应分开。** 在相同 160 世界批量和同一 MuJoCo Warp 后端下重复，并比较首个物理子步分歧、接触后放大与不干预世界的结局翻转。只要结果依赖模拟器特权接触/地形状态，就标注为上界探针；实际 32 维策略尚无这类输入。只有证明高度/位置条件动作必要且存在可获得的观测，才设计预瞄或接触观测；若通用动作可行，先试最小控制/动作修正。
3. **在新冻结开发面板做完整配对回归。** 原 GPU、terrain-v3 与候选同场景、同后端、同批量及冻结归一化；困难场景、原能力回归和 mixed 均逐例比较，多次重复并改变一次共同世界顺序。现有 48 例以原 GPU 的 35/48 为参照，至少需 **45/48 成功、46/48 完成**，且原场景逐例不退化；新面板应在看结果前按“相对同场景基线至少 +20 个百分点、困难成功至少 85%、完成至少 95%”重新锁定门槛。不能靠降低基线、调整姿态门或只选最好一次通过。
4. **只有候选行为与回归稳定过门，才准备正式训练。** 冻结新开发/原回归/最终留出及源码和检查点哈希；保持 1024 并行、原训练动画窗口打开和全部环境总览；成功率与原场景回归优先选模，连续 3 轮无实质改善停止、最多 10 轮。最终仅一次使用 720000～723999 全新留出验收，不用于调参或继续训练。GPU 物理吞吐不能称为 PPO 端到端加速。

进阶地形遵循 [terrain-v4 计划](TERRAIN_PLAN.md) 的单变量顺序：单侧上坡、纵向错相粗糙路、单侧台阶/路缘、摩擦差，再组合；单侧坡要求指定轮接触、两轮越过坡尾和机身安全。整车连续坡与世界坐标固定 5° 姿态门可能冲突，必须先明确姿态参考与安全边界。用户已明确“斜坡自旋”是**上坡后水平平台的转向/调平评估**，不是地形训练场景；当前直行指令接口不具备该转向任务的目标定义，不要混入下一轮 PPO 课程。

## 操作边界

- 本仓库 [项目约束](../AGENTS.md) 要求每项任务先应用 [wheelleg-project-guard](../.agents/skills/wheelleg-project-guard/SKILL.md)、按需读项目记忆。GPU 基线在 `wheelleg_warp/`；`wheelleg_ppo/` CPU 原基线和历史结果保留，不经配对校对不替换。
- 新实验用独立结果目录，不覆盖既有协议、失败记录或终评。`trace_policy_pair_v2.py`、`probe_prefix_action_v2.py`、`probe_hard_prefix_v2.py` 都接受必需的 `--output`，并要求目标目录不存在；它们是**复现旧问题**的诊断脚本，不是自动训练入口。
- 编辑前停止 `wheelleg-git-sync.service`，提交时包含 `PROJECT_MEMORY.md` 并保留 `.githooks/commit-msg`；结束恢复服务并核对远端 HEAD 与本地一致。不得强推或替他人处理冲突。
